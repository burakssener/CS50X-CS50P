int get_deposit(int num)
{
    int input;
    while(True)
    {
        printf("Determine the deposit please");
        scanf("%d", input);
        if ( input > 0)
        {
            break;
        }
        else
        {
            printf("please )
        }
    }
    return input;
}