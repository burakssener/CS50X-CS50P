answer = input("Plate: ").strip()

if not (6 >= len(answer) >=2):
    print("Invalid")
elif not (answer[0:2].isalpha()):
    print("Invalid")
elif not (answer[-1].isalpha() and for letter in answer[1:5] if letter)
